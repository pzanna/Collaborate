# Web server dependencies for Collaborate platform
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6

# Additional dependencies that might be missing from current setup
pydantic>=2.5.0
python-jose[cryptography]>=3.3.0  # For JWT tokens (future auth)
passlib[bcrypt]>=1.7.4  # For password hashing (future auth)
