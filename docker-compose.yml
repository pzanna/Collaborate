# Eunice Research Platform - Version 0.3 Microservices Architecture
# Root-level orchestration following the Version 0.3 design document

services:
  # Core Services

  # 1. API Gateway Service (Port 8001) - Only HTTP endpoint for clients
  api-gateway:
    build: ./services/api-gateway
    container_name: eunice-api-gateway
    ports:
      - "8001:8001"
    environment:
      - HOST=0.0.0.0
      - PORT=8001
      - MCP_SERVER_HOST=mcp-server
      - MCP_SERVER_PORT=9000
      - AUTH_SERVICE_URL=http://auth-service:8013
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/eunice
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
      - auth-service
      - postgres
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # 2. Enhanced MCP Server Service (Port 9000) - WebSocket hub for all agents
  mcp-server:
    build: ./services/mcp-server
    container_name: eunice-mcp-server
    ports:
      - "9000:9000"
    environment:
      - LOG_LEVEL=INFO
      - PORT=9000
      - HOST=0.0.0.0
      - REDIS_URL=redis://redis:6379
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/eunice
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - redis
      - postgres
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import socket; s = socket.socket(); s.connect(('localhost', 9000)); s.close()",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # 3. AI Service (Pure MCP Client)
  ai-service:
    build: ./services/ai-service
    container_name: eunice-ai-service
    environment:
      - MCP_SERVER_URL=ws://mcp-server:9000
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - XAI_API_KEY=${XAI_API_KEY}
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
      - HEALTH_PORT=8010
    ports:
      - "8010:8010" # Health check API
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8010/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  # Pure MCP Agent Services (No HTTP ports)

  # Research Manager Agent - Pure MCP Client
  research-manager-agent:
    build: ./agents/research-manager
    container_name: eunice-research-manager
    ports:
      - "8002:8002"
    environment:
      - SERVICE_HOST=0.0.0.0
      - SERVICE_PORT=8002
      - MCP_SERVER_URL=ws://mcp-server:9000
      - AGENT_TYPE=research_manager
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Literature Search Agent - Pure MCP Client
  literature-agent:
    build: ./agents/literature
    container_name: eunice-literature-agent
    ports:
      - "8003:8003"
    environment:
      - SERVICE_HOST=0.0.0.0
      - SERVICE_PORT=8003
      - MCP_SERVER_URL=ws://mcp-server:9000
      - AGENT_TYPE=literature
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8003/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Screening PRISMA Agent - Pure MCP Client
  screening-agent:
    build: ./agents/screening
    container_name: eunice-screening-agent
    ports:
      - "8004:8004"
    environment:
      - SERVICE_HOST=0.0.0.0
      - SERVICE_PORT=8004
      - MCP_SERVER_URL=ws://mcp-server:9000
      - AGENT_TYPE=screening
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8004/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Planning Agent - Pure MCP Client
  planning-agent:
    build: ./agents/planning
    container_name: eunice-planning-agent
    ports:
      - "8007:8007"
    environment:
      - SERVICE_HOST=0.0.0.0
      - SERVICE_PORT=8007
      - MCP_SERVER_URL=ws://mcp-server:9000
      - AGENT_TYPE=planning
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8007/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Executor Agent - Pure MCP Client
  executor-agent:
    build: ./agents/executor
    container_name: eunice-executor-agent
    ports:
      - "8008:8008"
    environment:
      - SERVICE_HOST=0.0.0.0
      - SERVICE_PORT=8008
      - MCP_SERVER_URL=ws://mcp-server:9000
      - AGENT_TYPE=executor
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8008/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Memory Agent - Pure MCP Client
  memory-agent:
    build: ./agents/memory
    container_name: eunice-memory-agent
    ports:
      - "8009:8009"
    environment:
      - SERVICE_HOST=0.0.0.0
      - SERVICE_PORT=8009
      - MCP_SERVER_URL=ws://mcp-server:9000
      - AGENT_TYPE=memory
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8009/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Synthesis & Review Agent - Pure MCP Client
  synthesis-agent:
    build: ./agents/synthesis
    container_name: eunice-synthesis-agent
    ports:
      - "8005:8005"
    environment:
      - SERVICE_HOST=0.0.0.0
      - SERVICE_PORT=8005
      - MCP_SERVER_URL=ws://mcp-server:9000
      - AGENT_TYPE=synthesis
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8005/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Writer Agent - Pure MCP Client
  writer-agent:
    build: ./agents/writer
    container_name: eunice-writer-agent
    ports:
      - "8006:8006"
    environment:
      - SERVICE_HOST=0.0.0.0
      - SERVICE_PORT=8006
      - MCP_SERVER_URL=ws://mcp-server:9000
      - AGENT_TYPE=writer
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8006/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Database Agent - Pure MCP Client
  database-agent:
    build: ./agents/database
    container_name: eunice-database-agent
    ports:
      - "8011:8011"
    environment:
      - SERVICE_HOST=0.0.0.0
      - SERVICE_PORT=8011
      - MCP_SERVER_URL=ws://mcp-server:9000
      - AGENT_TYPE=database
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/eunice
      - LOG_LEVEL=INFO
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - mcp-server
      - postgres
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8011/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Supporting Services

  # Database Service (Pure Database Management - No API)
  database-service:
    build: ./services/database
    container_name: eunice-database
    environment:
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/eunice
      - REDIS_URL=redis://redis:6379
      - LOG_LEVEL=INFO
      - HEALTH_CHECK_INTERVAL=30
      - MAINTENANCE_INTERVAL=3600
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - postgres
      - redis
    restart: unless-stopped

  # Docker Socket Proxy (Security Layer for Docker API Access)
  docker-socket-proxy:
    image: lscr.io/linuxserver/socket-proxy:latest
    container_name: eunice-docker-socket-proxy
    environment:
      - LOG_LEVEL=info
      - CONTAINERS=1 # Allow listing and managing containers
      - SERVICES=1 # Allow listing and managing services
      - NETWORKS=1 # Allow listing networks
      - VOLUMES=1 # Allow listing volumes
      - IMAGES=1 # Allow listing images
      - INFO=1 # Allow system info
      - VERSION=1 # Allow version info
      - PING=1 # Allow ping
      - EVENTS=1 # Allow events
      - EXEC=0 # Disable exec for security
      - SYSTEM=0 # Block system-level API access
      - BUILD=0 # Disable build operations
      - COMMIT=0 # Disable commit operations
      - POST=0 # Make API read-only for extra security
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - eunice-microservices
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /run
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:2375/_ping",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s

  # Authentication Service (JWT & RBAC - Port 8013)
  auth-service:
    build: ./services/auth-service
    container_name: eunice-auth-service
    ports:
      - "8013:8013"
    environment:
      - HOST=0.0.0.0
      - PORT=8013
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/eunice
      - SECRET_KEY=${AUTH_SECRET_KEY:-generate-a-secure-key}
      - ALGORITHM=HS256
      - ACCESS_TOKEN_EXPIRE_MINUTES=30
      - REFRESH_TOKEN_EXPIRE_DAYS=7
      - DEBUG=false
      - DOCKER_HOST=tcp://docker-socket-proxy:2375
      - ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:8001,http://127.0.0.1:5176
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - PYTHONNOUSERSITE=1
    networks:
      - eunice-microservices
    depends_on:
      - postgres
      - docker-socket-proxy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8013/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Supporting Infrastructure

  # Redis (Message Broker & Cache)
  redis:
    image: redis:7-alpine
    container_name: eunice-redis
    ports:
      - "6380:6379" # Use different external port to avoid conflict
    networks:
      - eunice-microservices
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: eunice-postgres
    ports:
      - "5433:5432" # Use different external port to avoid conflict
    environment:
      - POSTGRES_DB=eunice
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - eunice-microservices
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Development Tools (Optional)

  # Nginx (Load Balancer/Reverse Proxy + Frontend)
  nginx:
    image: nginx:alpine
    container_name: eunice-nginx
    ports:
      - "80:80"
    volumes:
      - ./infrastructure/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./frontend/dist:/usr/share/nginx/html:ro
    networks:
      - eunice-microservices
    depends_on:
      - api-gateway
    restart: unless-stopped
    profiles:
      - production
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/nginx-health"]
      interval: 30s
      timeout: 10s
      retries: 3

# Networks
networks:
  eunice-microservices:
    driver: bridge
    name: eunice-microservices
    labels:
      - "com.eunice.network=microservices"
      - "com.eunice.version=v0.3"

# Volumes
volumes:
  postgres-data:
    name: eunice-postgres-data
    labels:
      - "com.eunice.volume=database"
